{
  "name": "rest",
  "version": "1.0.0",
  "description": "rest api with zero",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json"
  },
  "dependencies": {
    "json-server": "^0.14.0"
  }
}
